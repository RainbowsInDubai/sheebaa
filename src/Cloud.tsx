import React from "react";

import cloudSvg from "./cloud.svg"; // Adjust the path accordingly

const Cloud = () => {
  return <img src={cloudSvg} alt="Cloud" className="cloud" />;
};

export default Cloud;
